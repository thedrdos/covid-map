import get_maps as gmimport get_data as gdimport picklefrom time import time as now# %% Load datat0=now();country_dnames = gd.get_countries()state_dnames   = gd.get_states()county_dnames  = gd.get_counties()print(now()-t0)t0=now();# country_data = gd.load(country_dnames)# state_data   = gd.load(state_dnames)# county_data  = gd.load(country_dnames)# print(now()-t0)# t0=now();# country_maps = gm.get_maps('country')# county_maps  = gm.get_maps('county')# state_maps   = gm.get_maps('state')# print(now()-t0)# data_path = './tmp_data/'# pickle.dump([#     country_data,#     state_data,#     county_data,#     country_maps,#     state_maps,#     country_maps],open(data_path+'tmp_save_vars.p','wb'))data_path = './tmp_data/'country_data, state_data, county_data, country_maps, state_maps, country_maps = pickle.load(open(data_path+'tmp_save_vars.p','rb'))country_mnames = country_maps.NAME.tolist()country_menames = country_maps.NAME_EN.tolist()country_both_names = [m for m in country_mnames if m in country_dnames]country_just_dnames  = [d for d in country_dnames if d not in country_mnames]country_just_enames  = [d for d in country_dnames if d not in country_menames]country_just_denames = [d for d in country_dnames if d not in country_mnames+country_menames]